# Local (no Docker) default DSN
DATABASE_URL="mysql://root:2342sk@127.0.0.1:3306/mabiguild"

# Docker note: web service overrides DATABASE_URL to use host `db`
# DATABASE_URL="mysql://root:2342sk@db:3306/mabiguild"

ROOT_DOMAIN="platform.localhost"
NEXTAUTH_URL="http://platform.localhost:3000"
NEXTAUTH_SECRET="change-me-in-production"
NODE_ENV="development"
